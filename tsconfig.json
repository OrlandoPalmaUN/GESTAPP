{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./packages/config/typescript/node.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": "."
  },
  "include": ["scripts/**/*.ts"],
  "exclude": ["node_modules", "apps", "packages", "dist"]
}

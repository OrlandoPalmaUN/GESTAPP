[build]
cmd = "pnpm install --frozen-lockfile && pnpm --filter @antigravity/api build"

[[build.phases]]
name = "setup"
cmds = ["pnpm config set store-dir .pnpm-store"]

[start]
cmd = "node apps/api/dist/server.js"

[env]
NODE_ENV = "production"

[nixpacks]
nodejs = "22"
